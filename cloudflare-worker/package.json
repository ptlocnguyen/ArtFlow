{
  "name": "artflow-pos-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:apps-script": "wrangler secret put APPS_SCRIPT_URL"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
